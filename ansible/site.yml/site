---
- name: Deploy Flask App
  hosts: app_server
  vars:
    domain_name: your-domain.com  # site.yml
    email_address: # ifeomalcollins@gmail.com
    workspace: "{{ lookup('env', 'WORKSPACE') | default('/tmp/workspace', true) }}"
  roles:
    - nginx_certbot
    - mongodb
    - flask_app